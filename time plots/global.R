


# create sumData global variable - can be treated the same as myDFo



# reloadSumFunction<- function(x){
#   outputListsumData <- lapply(outputList, "[[", "sumData")
#   sumData <- rbindlist(outputListsumData) # reload sumData after each session
# return(sumData)
# }

#sumData <- reloadSumFunction()